---
listen_address: ":23222"
verbose: false
tls_key: "certs/key.pem"
tls_crt: "certs/cert.pem"
commands:
  - cmd: echo
    args: ["banana", "tomato"]
    match_labels:
      "env": "testing"
      "owner": "me"
  - cmd: /bin/true
    match_labels:
      "beep": "boop"
